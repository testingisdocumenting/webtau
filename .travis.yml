language: java

cache:
  directories:
  - $HOME/.m2
  - $HOME/.npm
  - node_modules

dist: xenial

services:
  - xvfb

addons:
  chrome: stable

script: mvn -B package

deploy:
  - provider: script
    script: ./update-gh-pages.sh
    on:
      all_branches: true
      tags: true
