url = "http://localhost:8180"
configValue = 'testUser'
environments {
   dev {
       url = "http://dev.host:8080"
   }

   prod {
       reportGenerator = { report ->
           throw new RuntimeException("report issue " + entries.testEntries.size())
       }
   }
}